<center><h1>Item Comparison</h1></center>
<%= link_to 'Go back to navigation', :back, {:style => 'position:absolute; top:0;left:0;'}%>
<%# Descriptions %>
<div class="cell">
<div class="first">
	<br><br><br><br>
<h2>Product Features</h2>
</div>
<ul>
	<% session[:productType].constantize::InterestingFeatures.each do |column| %>
	    <li><%= column.gsub(/_id$/, '').gsub(/str$/,' ').gsub(/_/,' ').capitalize %></li>
	<% end %>
</ul>
</div>

<%= render :partial => 'product', :collection => @products %>

<%# This moves the border to the bottom %>
<div class="clear"></div>