<div class="outertitle"><div class="columntitle">
</div></div>

<% gray = true %>
<% $Continuous["filter"].each do |feat| %>
<div class="cell <%= gray ? "graybg" : "whitebg" %>">
	<% gray = !gray %>
	<% if numberofproducts > 1 && (@bestvalue[feat] != nil && (@bestvalue[feat] == product.id || IsPresentAndUniqueInCSV(product.id, @bestvalue[feat]))) %>
		<!-- If the interesting feature should be displayed then display -->
		<b><%= ContSpec.cache(product.id, feat).to_s + " (" + t("products.#{feat}unit") %>)</b>		
	<% else %>
		<%= ContSpec.cache(product.id, feat).to_s + " (" + t("products.#{feat}unit") %>)
	<% end %>
	</div>
<% end %>
<% $Categorical["filter"].each do |feat| %>
<div class="cell <%= gray ? "graybg" : "whitebg" %>">
	<% gray = !gray %>
	<% if numberofproducts > 1 && (@bestvalue[feat] != nil && (@bestvalue[feat] == product.id || IsPresentAndUniqueInCSV(product.id, @bestvalue[feat]))) %>
		<!-- If the interesting feature should be displayed then display -->
		<b><%= CatSpec.find_by_product_id_and_name(product.id, feat).value if CatSpec.find_by_product_id_and_name(product.id, feat) %></b>		
	<% else %>
		<%= CatSpec.find_by_product_id_and_name(product.id, feat).value if CatSpec.find_by_product_id_and_name(product.id, feat) %>
	<% end %>
	</div>
<% end %>
<% $Binary["filter"].each do |feat| %>
<div class="cell <%= gray ? "graybg" : "whitebg" %>">
	<% gray = !gray %>
	<% if numberofproducts > 1 && (@bestvalue[feat] != nil && (@bestvalue[feat] == product.id || IsPresentAndUniqueInCSV(product.id, @bestvalue[feat]))) %>
		<!-- If the interesting feature should be displayed then display -->
		<b><%= BinSpec.find_by_product_id_and_name(product.id, feat).value if BinSpec.find_by_product_id_and_name(product.id, feat) %></b>		
	<% else %>
		<%= BinSpec.find_by_product_id_and_name(product.id, feat).value if BinSpec.find_by_product_id_and_name(product.id, feat) %>
	<% end %>
	</div>
<% end %>
