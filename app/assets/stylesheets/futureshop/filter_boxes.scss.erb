@import 'shareddefs';
/*--------------------------------------//
//             Filter Boxes             //
//--------------------------------------*/
.optemo {
  #filter_form {
    padding-bottom: 5px;
  }
  .selection_label {
    font-weight: bold;
  }
  .remove_filter {
    font-size: 10px;
    div {
      width: 9px;
      height: 9px;
      vertical-align: middle;
      display: inline-block;
      background-image: url(<%= asset_data_uri "futureshop_remove_facet.gif" %>);
    }
  }
  #IE .label{
    padding-top: 12px;
  }
  .ie67 .remove_filter div {
    background: url(<%= asset_path "futureshop_remove_facet.gif" %>) no-repeat top left;
    display: inline;
  }
  .label {
  	text-align: left;
  	clear: left;
  	padding: 16px 0 4px 16px;
  }
  #selections_text .label {
    padding: 0;
  }
  #your_selections {
    margin-bottom: 8px;
  	padding: 8px 0;
  	border: 1px solid #DEE8F4;
  	@include background-gradient(#ffffff, #dee8f4);
  }
  .unitdisp {      /* Display units of features in filters */
  	font-size: 0.8em;
  	font-weight: normal;
  }
  .feature {
    font-size: 12px;
  }
  .ie6 .feature {
    position: relative;
  }
  .desc {
  	display: none;
  	position: absolute;
  	bottom: 1.5em;
  	left: 0;
  	padding: 4px;
  	border: solid thin black;
  	background-color: #fff;
  	z-index: 1001;
  }
  .hist {
  	position: absolute;
  	left: 20px;
  	top: 1px;
  	text-align: left;
  }
  #IE.ie6 .hist {
    left: 16px;
  }
  .boldlabel {
  	font-weight: bold;
  	text-align: center;
  	clear: left;
    padding: 6px 0 0;
    background-color: #DFDED5;
    margin: 8px 0 2px -6px;
    border-top: 1px #ccc solid;
    border-bottom: 1px #ccc solid;
    height: 23px;
  }
}